{
  "$id": "https://orca.sih26176/schemas/TimeWindow.json",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "TimeWindow",
  "description": "Interpreted analysis time window, always stored in both local (IST) and UTC. §14, §20.1",
  "type": "object",
  "required": ["local", "utc"],
  "properties": {
    "local": {
      "type": "string",
      "description": "ISO 8601 interval 'start/end' in local time, e.g. '2026-09-12T05:00:00+05:30/2026-09-12T11:00:00+05:30'."
    },
    "utc": {
      "type": "string",
      "description": "Same interval expressed in UTC."
    },
    "original_expression": {
      "type": ["string", "null"],
      "description": "Natural-language time phrase as given by the user, e.g. 'tomorrow morning'. Null if an explicit date/time was supplied."
    },
    "matched_bucket": {
      "type": ["string", "null"],
      "enum": ["early_morning", "morning", "afternoon", "evening", "night", "today", "tomorrow", null],
      "description": "Default time bucket matched from §14.1, if the expression matched one."
    }
  },
  "additionalProperties": false
}
